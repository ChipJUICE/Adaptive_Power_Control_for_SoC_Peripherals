# DEPS.yml - Dependency configuration for Power-Aware Peripheral Controller
# This file defines simulation targets for the project modules

# Activity Counter Testbench
bench_activity_counter:
  deps:
    - activity_counter.sv
    - tb_activity_counter.sv
  top: tb_activity_counter

# Configuration Registers Testbench
bench_cfg_regs:
  deps:
    - cfg_regs.sv
    - tb_cfg_regs.sv
  top: tb_cfg_regs
